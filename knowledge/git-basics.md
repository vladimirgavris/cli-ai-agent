# Git basics

Git tracks changes in a project over time. A commit is a saved checkpoint.
A branch is a separate line of work that can later be merged.

Safe inspection commands:
- git status: shows the current branch and changed files.
- git log --oneline: shows recent commits.
- git diff: shows unstaged changes.

git add selects changes for the next commit. It does not upload anything.